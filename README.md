# ansible config

## how to run

```
python3 -m ansible-runner -a deploy -u username
```

status:
mongodb:

standalone: done

replicaset: done

shard cluster: need to test

elasticsearch:

standalone: done

cluster: done

kafka:

with zookeeper: done

with kraft: done setup(need to test)
