# ansible config

## how to run

```
python3 -m ansible-runner -a deploy -u username
```

status:
mongodb:

standalone: done

replicaset: done

shard cluster: need to test

elasticsearch:

standalone: done

cluster: need to test
