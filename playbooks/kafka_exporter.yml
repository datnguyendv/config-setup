---
- name: Install kafka exporter
  hosts: all
  become: true
  gather_facts: true
  roles:
  - ../roles/kafka-exporter

- name: Register to metric server
  hosts: all
  become: yes
  roles:
    - ../roles/metric-register
  vars:
    consul_service_name: "kafka"
    consul_service_port:  "9308"
